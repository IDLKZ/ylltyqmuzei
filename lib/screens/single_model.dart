import 'package:flutter/material.dart';


class SingleModel extends StatelessWidget {
  const SingleModel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
