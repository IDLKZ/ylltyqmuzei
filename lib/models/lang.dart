import 'package:get/get.dart';
class Lang{
  final String noTranslation = 'отсутсвует перевод';
  getTitle(){
    var locale = Get.locale!.languageCode;
    switch(locale){
      case 'ru' {
        return
      }
    }
  }
}